apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: task
  labels:
    app: web
  annotations:
    tailscale.com/expose: "true"
spec:
  type: NodePort
  selector:
    app: web
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 80

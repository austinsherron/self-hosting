apiVersion: v1
kind: Service
metadata:
  name: webui-svc
  labels:
    app: webui
spec:
  type: NodePort
  selector:
    app: webui
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30080

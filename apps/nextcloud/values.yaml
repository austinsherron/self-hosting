image:
  repository: quay.io/e_minguez/nextcloud-container-nfs-fix
  tag: 30.0.3

nextcloud:
  host: nextcloud-nextcloud
  
  persistence:
    subPath: nextcloud

persistence:
  nextcloudData:
    enabled: true
    storageClass: nfs
    size: 50Gi


livenessProbe:
  initialDelaySeconds: 240

readinessProbe:
  initialDelaySeconds: 240

service:
  annotations:
    tailscale.com/expose: "true"

ingress:
  className: tailscale
  enabled: true
